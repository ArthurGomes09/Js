var peso1 = 1.3
var peso2 = Number("2.0")

console.log(peso1, peso2)
console.log(Number .isInteger(peso1)) // pergunta se a variável é inteiro, Number sempre escrito com o N maiúsculo .isInteger > pergunta se é inteiro. Se for inteiro define como "True" se o Contrário "False".
console.log(Number .isInteger(peso2))
